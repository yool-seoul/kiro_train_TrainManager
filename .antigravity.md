# Train Manager — Antigravity CLI 워크스페이스 컨텍스트

이 파일은 Antigravity CLI(`agy`) 가 이 워크스페이스에서 우선적으로 읽는 컨텍스트 파일입니다.
본 프로젝트는 **Antigravity (AGY)**, **OpenAI Codex CLI**, **Kiro** 가 동일한 로컬 워크스페이스와
Git 저장소(`main` 브랜치)를 공유하며 협업합니다. 공통 규칙은 저장소 루트의
[`AGENTS.md`](./AGENTS.md) 에 정의되어 있으며, 이 문서를 그대로 따릅니다.

---

## 1. 기본 언어 및 커뮤니케이션 정책
- **한국어 우선**: 사용자와의 모든 대화, 설명, 보고는 **한국어를 기본 언어로 우선** 사용한다.
- 코드 식별자, 라이브러리명, 터미널 명령어, Git 커밋 메시지 등은 표준 영문/한글 규칙을 준수한다.

## 2. 감사 로그 정책 (Audit Logging)
- 저장소 루트의 `audit.md` 에 **모든 대화 턴을 누적 기록**한다.
- 매 턴 사용자 요청을 처리한 뒤, 최종 응답 전에 `audit.md` 끝에 새 항목을 append 한다.
- 항목 형식:
  ```markdown
  ## Turn N — <짧은 제목>
  **사용자**: <요청 요약>
  **AI**: <수행한 작업/응답 요약, 변경 파일, 검증 결과>
  ```
- 기존 내용을 덮어쓰지 않고 항상 마지막 번호에 이어서 **append** 만 수행한다.
- 비밀번호, API 토큰, 인증 세션 키 등 **민감한 시크릿 값은 절대 기록하지 않는다**.

## 3. 개발 환경 및 실행 가이드
- **Python 버전**: Python 3.10 이상 (3.13 권장)
- **가상환경**: 프로젝트 루트의 `.venv` 가상환경 사용
- 테스트 실행: `.venv/bin/python -m pytest tests/ -v`
- 로컬 서버 실행: `.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000`
- `.env.example` 을 복사해 `.env` 생성 후 설정. `TRAINMGR_DATA_SOURCE` 를 `mock`/`live` 로 전환하며,
  변경 시 서버 재시작 필수.

## 4. 에이전트 협업 및 파일 수정 원칙
- 모든 에이전트는 동일한 로컬 워크스페이스와 Git 저장소(`main` 브랜치)를 공유한다.
- 파일 수정 전 `git status` 또는 최신 상태를 확인하여 충돌을 방지한다.
- 기존 주석, 독스트링 및 코드 스타일을 최대한 보존한다.

---

세부 내용이나 정책이 변경되면 `AGENTS.md` 를 1차 소스로 갱신하고, 이 파일도 함께 동기화한다.

# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9          # form 데이터 파싱
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
itsdangerous>=2.1,<3.0           # SessionMiddleware 서명

# --- Google Sheets 자격증명 (D3) ---
gspread>=6.0,<7.0
google-auth>=2.29,<3.0

# --- 실제 열차 연동 (data_source=live) ---
# Python 3.10+ 필요.
korail2-ncard>=0.1,<1.0     # KTX/Korail (anti-bot 보완 fork). import 이름은 korail2
SRTrain>=2.0,<3.0           # SRT. import 이름은 SRT
pycryptodome>=3.20,<4.0     # korail2 로그인 암호화(AES)

# --- 개발/테스트 ---
pytest>=8.0,<9.0
httpx>=0.27,<1.0                 # TestClient 용
